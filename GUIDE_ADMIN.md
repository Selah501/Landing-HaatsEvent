# 📘 통합 관리자 매뉴얼 (CRM 및 스마트 캘린더)

본 매뉴얼은 현장 소장님과 관리자가 **고객 상담(AI/웹)**부터 **최종 캘린더 예약 확정**까지의 모든 흐름을 쉽고 정확하게 처리할 수 있도록 작성된 사용자 가이드입니다.

---

## 1. CRM 화면 (기본 설정 페이지 - `admin.html`)

기본 설정 페이지 하단의 **[🌐 웹(QR) 접수 및 DB 마스터 표]**는 모든 고객의 예약 현황과 AI 통화 요약이 집결되는 중앙 통제실입니다.

### 💡 주요 기능 및 사용법

1. **중복 통화 고객 자동 묶음 (히스토리 기능)**
   - 동일한 고객(같은 전화번호)이 여러 번 전화를 걸어 AI와 상담한 경우, 표나 카드에 여러 개가 뜨지 않고 **가장 최근 통화 1건**만 메인으로 노출됩니다.
   - 카드 하단의 `🔄 과거 이력 (N건) 보기` 버튼을 누르면 고객의 이전 1차, 2차 통화 요약을 팝업으로 한눈에 확인할 수 있습니다. 고객이 변심했거나 추가로 요구한 사항의 흐름을 놓치지 마세요!

2. **상태 뱃지와 타겟 날짜 확인**
   - 예약의 현재 상태가 직관적인 색상의 뱃지(`🟡 확정 대기중`, `🟢 예약 확정`, `🔴 취소/기타`)로 표시됩니다.
   - 뱃지 바로 아래에는 캘린더에 배정된 **실제 예약 날짜와 시간 (예: 🕒 8/10 4pm)**이 표기되어, 굳이 달력을 열지 않아도 언제 예약된 건인지 바로 알 수 있습니다.

3. **수기(문자) 예약 생성**
   - AI 통화 없이 개인 휴대폰으로 바로 문자가 와서 예약을 잡아야 할 때 사용합니다.
   - 마스터 표 제목 옆의 `➕ 수기(문자) 예약 생성` 주황색 버튼을 누르면 즉시 예약 팝업이 뜹니다.

4. **예약 날짜/시간 변경 (안전한 이동)**
   - 이미 예약된 건의 일정을 바꿔야 할 때는 우측의 `🗓️ 예약 관리 (문자발송)` 연두색 버튼을 누르세요.
   - 팝업이 열릴 때 오늘 날짜가 아닌 **원래 예약되어 있던 날짜와 시간**이 자동으로 세팅된 상태로 열립니다. 여기서 날짜만 달력에서 수정하고 하단의 `저장`을 누르면 안전하게 일정이 이동합니다.

5. **오류 데이터 강제 삭제**
   - 테스트를 하다 꼬인 데이터나, 캘린더에는 없는데 표에만 남아있는 유령 데이터가 있다면 우측의 `🗑️ 강제 삭제 (오류건)` 빨간색 버튼을 눌러 영구 삭제할 수 있습니다.

---

## 2. 작업지시 및 캘린더 화면 (`admin_v2.html`)

태블릿이나 PC를 켜두고 현장에서 당일의 일정을 관리하는 달력 뷰입니다.

### 💡 주요 기능 및 사용법

1. **읽기 전용 달력으로 빈자리 찾기**
   - CRM 화면에서 예약을 잡기 전, "언제 자리가 비었지?" 확인만 하고 싶을 때는 CRM 패널 상단의 `🗓️ 달력 보기 (읽기 전용)` 회색 버튼을 누르세요.
   - 팝업으로 달력이 열리지만, 실수로 칸을 누르더라도 상태가 변하거나 예약이 꼬이지 않는 '눈으로만 보는' 안전한 달력입니다.

2. **비밀번호 없는 쾌적한 진입**
   - 이미 CRM 화면(기본 설정 페이지)에 로그인해서 들어와 있다면, 상단의 `⚙️ 달력 관리 (관리자 모드)` 보라색 버튼을 눌렀을 때 귀찮은 비밀번호(5096)를 다시 묻지 않고 즉시 관리자 달력으로 프리패스 입장합니다.

3. **달력 카드에서 AI 통화 요약 바로 보기**
   - 달력 화면에 떠 있는 고객 카드에는 `기타(메모)` 칸이 있습니다. 내용이 길면 공간을 위해 줄임말(`...`) 처리됩니다.
   - 그 바로 아래에 있는 **`📝 전체 메모 및 통화요약 보기`** 노란색 버튼을 누르시면, 해당 고객과 AI가 나눈 상담 요약 전체를 팝업으로 즉시 확인할 수 있습니다. 현장 방문 전 문 앞에서 빠르게 읽어보고 들어가세요!

---

## 🚀 완벽한 업무 흐름 (추천 동선)

1. 아침에 출근하여 **CRM 마스터 표**를 엽니다.
2. 새로 들어온 AI 통화 요약을 봅니다. 필요 시 `과거 이력 보기`를 눌러 이전 통화 내용도 숙지합니다.
3. `달력 보기 (읽기 전용)`을 띄워놓고 빈 시간을 확인한 뒤 고객에게 문자를 보내 확정합니다.
4. `🗓️ 예약 관리` 버튼을 눌러 확정된 시간으로 예약을 꽂아 넣습니다.
5. 현장으로 출발하기 전, **캘린더 화면(admin_v2.html)**을 태블릿으로 띄우고 `📝 통화요약 보기`를 눌러 고객의 특이사항을 마지막으로 점검한 뒤 현장 벨을 누릅니다!

---

## 3. 🔐 보안 및 시스템 관리 정보 (API Keys)

본 프로젝트는 통화 분석과 데이터베이스 연동을 위해 외부 클라우드 API를 사용하고 있습니다. 
만약 시스템이 갑자기 에러를 뿜거나 작동하지 않는다면, 아래 키들의 유출 및 차단 여부를 가장 먼저 확인하십시오. (※ **경고:** 이 정보는 절대 GitHub 등의 외부 공개 저장소에 업로드되어서는 안 됩니다.)

### 📌 Google Gemini API Key (통화 분석 AI)
- **발급처:** Google AI Studio (https://aistudio.google.com/)
- **프로젝트 명칭 (식별자):** `projects/95575136784` (Google Cloud Service Account)
- **현재 사용 중인 키:** `<SECRET_REMOVED_FOR_GITHUB>`
- **적용 위치:** PC 내 `c:\dev\전단지이벤트\landing\.env` 파일의 `GEMINI_API_KEY` 값
- **문제 발생 시 조치:** 구글 AI Studio에 접속하여 해당 프로젝트의 기존 키를 삭제하고 새 키를 발급받아 위 위치에 덮어씌웁니다. 그 후 PC 백그라운드의 `crm_daemon.py`를 재시작해야 합니다.

### 📌 Notion API Token (상품 카탈로그 및 시공대장 DB 연동)
- **발급처:** Notion API 통합(Integrations) 설정
- **적용 봇 이름:** `하츠랜딩페이지` (Internal Integration)
- **적용 위치:** (해당 기능을 사용하는 스크립트의 `.env` 파일 내부 `NOTION_API_KEY` 등)
- **문제 발생 시 조치:** 키가 유출되면 노션에서 즉각 자동 폐기하므로, 통합 설정에서 새 토큰을 복사하여 교체해야 합니다.
