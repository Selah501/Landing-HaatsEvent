# 🚀 하츠 충남대리점 V2 인계문서 (Handover Document)

**작성일시:** 2026년 08월 09일
**목적:** 새로운 대화창(세션)으로 넘어가기 전, 직전 세션에서 수행된 복잡한 DB 스키마 변경, 데이터 이관, UI 로직 수정 사항을 상세히 50줄 이상의 분량으로 기록하여, 다음 에이전트가 완벽하게 문맥을 이어받을 수 있도록 함. (토큰 최적화 및 히스토리 보존 목적)

---

## 1. 직전 세션 핵심 요약 (우려가 현실로: 토큰/세션 길이 한계 도달)
- 기존 대화 세션의 컨텍스트(Context Window)가 한계에 다다르면서, `grep` 이나 파일 읽기 등의 과정에서 에러 빈도가 늘고 속도가 저하되는 현상이 발생했습니다.
- 그럼에도 불구하고, 대표님께서 요청하신 "407개 제품 카탈로그 PDF 추출 -> Supabase DB 이관 -> Notion 동기화 -> 프론트엔드 카테고리 정상화"라는 가장 거대하고 위험도가 높은 (High Risk) 작업을 성공적으로 완료하고 배포까지 마쳤습니다.

---

## 2. 세부 코딩 및 작업 내역 (매우 상세)

### 2.1. PDF 카탈로그 기반 마스터 상품 DB 추출 및 이관
- **PDF 텍스트 및 이미지 추출:** Python 스크립트(`extract_all.py` 등)를 작성하여 "2026 하츠 유통 소매점 카다로그_F.pdf" 파일 내의 제품명, 모델명, 규격, 카테고리를 자동 추출했습니다.
- **분류 체계(대분류) 재정립:** 추출 시 페이지 범위에 따라 9개의 대분류(하츠후드 데코, 하츠후드 엘리카, 하츠후드 플래티늄, 하츠후드 시스템, 하츠 전기레인지, 하츠 가스레인지, 하츠 수전빌트인, 하츠 씽크볼, 하츠 기타빌트인)로 자동 할당하는 로직을 적용했습니다. 
- **DB 적재 (Supabase):** Node.js 스크립트(`import_parsed_to_supabase.js` 및 `import_all_catalogs.js`)를 작성하여 추출된 데이터를 Supabase의 `products` 테이블로 Bulk Insert 하였습니다.
- **이미지 호스팅 연동:** 로컬에 추출된 제품 이미지들을 Firebase Hosting `public/catalogs` 디렉토리에 복사한 후 `firebase deploy --only hosting`을 실행해 웹 상의 URL로 만들고, 이를 Supabase DB의 `details->level1->images` 경로에 매핑했습니다.

### 2.2. Notion 동기화 및 Firebase ID 빈칸 채우기 로직
- **Sync 스크립트 개선:** `sync_notion_to_supabase.js`를 구동하여 Supabase와 Notion 간의 데이터를 동기화하려 했으나, Notion 측에 `FirebaseID` 필드가 비어있어 스킵되는 문제가 발생했습니다.
- **Firebase ID 자동 할당:** 이를 해결하기 위해 `fill_firebase_ids.js`라는 일회성(One-off) 스크립트를 즉석에서 작성하여, Notion 데이터베이스를 스캔하고 FirebaseID가 없는 항목들에 대해 고유 UUID를 발급하여 업데이트했습니다. (대표님의 '인라인 스크립트 사용 금지' 룰을 준수하여 반드시 `.js` 파일로 저장 후 실행함)

### 2.3. admin.html "먹통(Freeze)" 버그 완벽 디버깅 및 수정
- **증상:** "1. 상품 마스터 관리"부터 2, 3, 4번 기능 탭 전체가 하얗게 멈추고 클릭이 되지 않는 현상 발생.
- **원인 추적:** 브라우저 콘솔상에서 발생했을 `window.currentProducts.forEach is not a function` 에러를 예측. Firebase Realtime Database(RTDB)는 배열 중간의 인덱스가 삭제되어 이빨이 빠진 형태(gap)가 되면 Array를 Object로 자동 변환하여 반환하는 고질적인 특성이 있습니다.
- **코드 수정:** `replace_file_content` 툴을 사용해 `admin.html`의 400번째 줄 부근 Firebase onValue 콜백 함수 내에서,
  `window.currentProducts = data.products || [];` 로 되어있던 코드를
  `window.currentProducts = Array.isArray(data.products) ? data.products : Object.values(data.products || {});` 로 변경하여 객체가 들어와도 강제로 배열로 캐스팅되도록 방어 코드를 추가했습니다.

### 2.4. admin_v2.html 작업 결과 보고서 "동적 카테고리 매핑" 구현
- **기존 문제:** `admin_v2.html`의 작업 결과 보고서 작성 모달 내 "시공된 제품 선택" 드롭다운이 하드코딩("하츠 후드", "하츠 욕실", "하츠 씽크", "힘펠 욕실" 등)되어 있어, 새로 추가된 9개 분류(데코, 엘리카, 플래티늄 등)가 전혀 노출되지 않았습니다. 브랜드(`p.brand`)가 공란으로 들어간 새 제품들이 `derivedCat = "기타"`로 빠지면서 목록에서 아예 증발해 버린 것입니다.
- **대표님 요구사항:** "디비의 대분류를 그냥 쓰면 돼. 앞으로 몇 개 카테고리 늘릴 거고, 9개 카테고리 이외의 대분류는 '기타카테고리'로 묶어달라."
- **해결 방안 기획:** 드롭다운 목록을 하드코딩에서 **"DB 기반 완전 동적(Dynamic) 생성"** 방식으로 전면 개편했습니다. 
- **Python 스크립트를 통한 정교한 치환:** Windows 환경에서의 인코딩(UTF-16 vs UTF-8) 충돌이나 줄바꿈(Regex) 이슈로 `replace_file_content`가 실패하는 상황을 회피하고자, `fix_admin_logic_v4.py` 스크립트를 직접 작성해 `admin_v2.html`의 특정 함수(`populateBrandCategories`와 `updateModalProductList`) 블록을 안전하게 덮어쓰도록 설계했습니다.
- **구현된 최종 자바스크립트 로직:**
  1. **구형(Legacy) 카테고리 필터링:** `const oldCats = ['욕실 환풍기', '환기 시스템', '주방 후드', '주방 가전', '생활 에어가전', '욕실 환기가전', '싱크볼/수전', '빌트인 수전/싱크볼', '가스/전기 쿡탑', '기타'];` 배열을 선언하여 쓰레기값 및 과거 잔재 데이터를 식별합니다.
  2. **동적 드롭다운 렌더링 (`populateBrandCategories`):** `window.masterProducts` (Supabase 데이터)에서 중복 없는 `category` 리스트를 뽑아낸 뒤, `oldCats`에 포함되지 않은 카테고리들만 모아 정렬합니다. 마지막에 `'기타카테고리'`를 강제로 하나 추가하여 `select` 요소에 `option` 태그로 렌더링합니다. (현재는 새로 추출된 9개 카테고리만 정확히 표시됨)
  3. **제품 리스트 필터링 (`updateModalProductList`):** 특정 대분류를 선택했을 때, 해당 제품의 `category`가 `oldCats`에 해당하거나 비어있으면 무조건 `derivedCat = "기타카테고리"`로 짬처리(?)합니다. 반면, 정상적인 카테고리를 가지고 있으면 자기 자신의 `category`를 그대로 대분류명으로 사용하여 화면에 리스트업(매핑)합니다.
- **결과:** 앞으로 `masterProducts` DB에 '하츠 식기세척기' 같은 새로운 카테고리 텍스트가 생기더라도, 개발자가 코드를 수정할 필요 없이 **자동으로 드롭다운에 노출되는 확장성**을 확보했습니다.

### 2.5. 배포 완료 및 Alert 기능 연동
- 수정한 HTML 및 JS 코드들을 `firebase deploy --only hosting` 명령어로 실서버에 즉시 배포했습니다.
- 대기 상태로 전환될 때 사용자(대표님)에게 즉시 알림을 주기 위해, `.agents/AGENTS.md`에 정의된 룰을 따라 PowerShell을 이용해 시스템 알림음 스크립트(`play_alert.ps1`)를 백그라운드에서 실행하도록 구조화하였습니다.

---

## 3. 새로운 세션(에이전트)에게 전달하는 주의사항 및 Action Item

1. **Supabase가 메인 DB입니다:** 현재 상품 마스터 데이터의 진실의 원천(Source of Truth)은 Firebase RTDB가 아니라 **Supabase `products` 테이블**입니다. 
2. **동기화 파이프라인 명심:** Notion DB 변경 -> Supabase 동기화 -> `admin_v2.html`에서 Fetch 하는 흐름을 완벽히 숙지하고 작업에 임해야 합니다. (Node.js 함수 폴더 내의 `.js` 스크립트들이 이 역할을 담당함)
3. **인라인 스크립트 금지:** 팝업 승인 스트레스를 줄이기 위해 터미널에서 `node -e` 나 `python -c` 등의 인라인 실행을 지양하고, 반드시 스크래치 파일(`.js`, `.py`)을 쓴 뒤 그것을 실행할 것.
4. **넥스트 스텝 (차후 과제):** 
   - `admin_v2.html` 외에, 아직 Firebase RTDB를 바라보고 있는 `admin.html`의 잔재 코드들 또는 타 페이지(예: CRM 연동 부분)에서 낡은 카테고리 방식을 쓰고 있는 곳이 없는지 통합 검토가 필요합니다.
   - 대표님께서 앞서 언급하신 "디비 관리 기능(상품 마스터 관리 화면 정상화 및 고도화)" 관련 추가 작업 지시가 있을 예정입니다. (이관된 400+ 데이터에 대한 CRUD 화면 보강 등)

---
*본 문서는 컨텍스트 단절을 방지하기 위해 생성되었습니다. AI는 다음 세션 시작 시 이 문서를 반드시 읽고 현재 아키텍처와 로직 구조를 파악하십시오.*
