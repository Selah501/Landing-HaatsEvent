# V2 예약 시스템 개발 요약 (작업 진행 내역)

본 문서는 `MASTER_PLAN.md`의 1단계 요구사항을 기반으로 진행된 `reservation_v2.html`의 개발 완료 내역을 요약합니다.

## 1. 독립적인 V2 예약 페이지 구성
- **파일 생성**: 기존 코드를 훼손하지 않고 완전히 분리된 `reservation_v2.html`을 신규 생성했습니다.
- **Firebase 연동**: Firebase RTDB(`reservations`, `products`, `events`) 데이터를 실시간(onValue)으로 읽고 쓸 수 있도록 뼈대를 구축했습니다.

## 2. 직관적인 예약 흐름 (순차적 UI)
고객이 가장 자연스럽게 느낄 수 있도록 **"날짜 선택 ➡️ 시간 선택 ➡️ 제품 및 연락처 입력"** 순서로 UI가 펼쳐지도록 구현했습니다.
- **14일 달력 UI**: 오늘부터 14일간의 날짜만 활성화되는 다크 테마 기반 달력을 구현했습니다.
- **시간표 그리드**: 09:00 ~ 17:00 슬롯을 제공하며, 각 슬롯은 예약 가능(회색), 대기중(노란색), 확정/차단(진한 회색/취소선) 상태를 색상으로 구분하여 표시합니다.

## 3. 제품별 '소요 시간' 기반 다중 슬롯 예약 (핵심 고도화)
단순 1시간 단위 예약을 넘어, 시술(제품)마다 다른 작업 시간을 반영하여 자동으로 여러 시간을 확보하는 기능을 개발했습니다.
- **관리자 설정**: `admin.html`의 상품 마스터 관리에 **'소요시간(분)' 필드를 추가**하여 관리자가 제품별 시간을 직접 통제할 수 있게 했습니다.
- **현장 맞춤형 상품 노출**: URL 파라미터(`?eventId=...`)를 인식해 해당 현장에 배정된 상품만 동적으로 화면에 렌더링합니다.
- **시간 자동 계산**: 고객이 상품을 다중 선택하면 시간을 합산하고, 1시간 단위로 올림하여 "작업은 총 X시간 소요됩니다."라고 즉시 안내합니다.
- **안전한 예약 검증**: 시작 시간부터 계산된 X시간 동안 빈 슬롯이 없거나(중간 차단), 18:00 영업 종료 시간을 초과할 경우 예약을 사전에 차단합니다.
- **다중 슬롯 예약 접수**: 검증을 통과하면 `reservationId`라는 고유 그룹 ID를 부여하여, 3시간 작업일 경우 3칸의 슬롯을 한 번에 **'대기(pending)'** 상태로 접수합니다.

## 4. 관리자 일괄 확정 기능 (Admin Mode)
- 관리자(PW: 5096) 모드에서 대기 중인 시간표 중 아무 칸이나 클릭하면, 동일한 `reservationId`로 묶인 모든 연속된 시간을 찾아 **한 번에 '최종 확정(confirmed)' 처리**합니다.
- 확정된 일정을 취소하거나 초기화할 때도 묶여있는 칸들이 일괄적으로 초기화되므로 관리가 매우 편리합니다.

---
**💡 향후 과제 (Next Steps)**
- 관리자 확정 시 고객에게 발송되는 '확정 문자 발송 로직' 연동
- 랜딩 페이지 메인(`index.html`)과 V2 예약 페이지의 매끄러운 통합 및 이동 동선 구축
